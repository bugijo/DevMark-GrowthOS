# OpenAPI gerado

Artefatos desta pasta serão gerados a partir de `/api/v1/openapi.json`. Não edite tipos gerados manualmente nem replique enums do domínio.

O gate de contrato deve falhar quando o OpenAPI mudar sem regenerar os consumidores versionados.

